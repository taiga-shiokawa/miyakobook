{
  "name": "business-sns",
  "version": "1.0.0",
  "description": "",
  "main": "backend/server.js",
  "scripts": {
    "dev": "nodemon backend/server.js",
    "start": "node backend/server.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "clean": "rm -rf frontend/dist",
    "build:fresh": "npm run clean && npm run build",
    "dev:clean": "npm run clean && npm run build && npm run dev",
    "test:load": "artillery run tests/load/load-test.yml",
    "test:load:report": "artillery run tests/load/load-test.yml -o report.json && artillery report report.json"
  },
  "keywords": [],
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.5.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2",
    "mailtrap": "^3.4.0",
    "mongoose": "^8.7.1",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
